# Phase 3 API dependencies
# Install with: pip install -r requirements.txt -r requirements-api.txt

# Litestar ASGI framework
litestar[standard]>=2.24.0

# ASGI server
uvicorn[standard]~=0.34

# Async Redis client + ARQ worker queue
redis~=5.2
arq~=0.26

# Fast DataFrame processing
polars>=1.0

# Async HTTP
httpx~=0.28

# JWT auth — >=2.13.0 fixes PYSEC-2026-175/177/178/179 (JWKS fetch DoS,
# algorithm confusion, detached JWS bypass)
PyJWT>=2.13.0

# Database ORM
SQLAlchemy[asyncio]~=2.0
asyncpg~=0.30

# Security-patched transitive deps — explicitly floored so pip install
# on a fresh venv lands safe versions.
aiohttp>=3.14.1        # PYSEC-2026-237, CVE-2026-34993 et al
cryptography>=49.0.0   # PYSEC-2026-36, GHSA-537c
idna>=3.18             # PYSEC-2026-215
lxml>=6.1.1            # PYSEC-2026-87
python-multipart>=0.0.32  # CVE-2026-53538/53539/53540
starlette>=1.3.1       # PYSEC-2026-248/249
urllib3>=2.7.0         # PYSEC-2026-141/142

# MCP server for the Lab page — talks to Claude Code via stdio.
# Only used by backend/mcp/kite_server.py (operator's local venv runs
# this as a subprocess from Claude Code); the deployed API never imports
# it. Pinned floor only — the protocol is still evolving.
mcp>=1.27

# Monthly investor-portal PDF statements. Pure-Python; no system deps
# (no libcairo / libpango). Used only by
# backend/api/algo/investor_statement.py.
fpdf2~=2.8

# Code-metrics capture (`scripts/capture_metrics.py`). All are pure-
# Python CLIs invoked as subprocesses by the capture script; the API
# itself never imports them.
#   radon              — cyclomatic complexity + raw LOC
#   vulture            — stale-code detector
#   pytest-json-report — per-test duration capture (--with-test-times)
# Pinned floors; the CLIs are stable.
radon>=6.0
vulture>=2.10
pytest-json-report>=1.5

# DESIGN_GUIDE.pdf generation (`generate_pdf.py`). pandoc + xelatex
# (BasicTeX / MacTeX) provide the LaTeX engine; pikepdf compresses
# the output. Optional at runtime — the script falls back gracefully
# if pikepdf is absent.
pikepdf>=8.0
