# Phase 3 API dependencies
# Install with: pip install -r requirements.txt -r requirements-api.txt

# Litestar ASGI framework
litestar[standard]~=2.12

# ASGI server
uvicorn[standard]~=0.34

# Async Redis client + ARQ worker queue
redis~=5.2
arq~=0.26

# Fast DataFrame processing
polars>=1.0

# Async HTTP
httpx~=0.28

# JWT auth
PyJWT~=2.10

# Database ORM
SQLAlchemy[asyncio]~=2.0
asyncpg~=0.30

# MCP server for the Lab page — talks to Claude Code via stdio.
# Only used by backend/mcp/kite_server.py (operator's local venv runs
# this as a subprocess from Claude Code); the deployed API never imports
# it. Pinned floor only — the protocol is still evolving.
mcp>=1.27

# Monthly investor-portal PDF statements. Pure-Python; no system deps
# (no libcairo / libpango). Used only by
# backend/api/algo/investor_statement.py.
fpdf2~=2.8
